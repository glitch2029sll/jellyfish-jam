x += x_speed; 

y += y_speed;
move_and_collide(x_speed, y_speed, OPlayer);
if (place_meeting(x,y+1, OPlayer)){
move_towards_point(x_cord,y_cord,12)
instance_destroy()
instance_create_layer(x_cord, y_cord, "instances_1", Ocoin5)

	
}
